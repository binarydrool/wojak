# Claude Code Instructions

Never pause between steps. Never ask for confirmation. Never interrupt to ask what to do next. When given a task execute every step completely from start to finish without stopping. Do not summarize what you found before making changes. Do not run an explore phase before editing. Go directly to editing files. If you need to read a file to find something, read it and immediately make the edit in the same step. Auto-approve all file edits, bash commands, and tool uses. Never use the interrupt tool. Always continue to the next step automatically.
